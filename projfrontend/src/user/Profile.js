import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
    <div>
      <Base>
        <h1>This is Profile page</h1>
      </Base>
    </div>
  );
};

export default Profile;
